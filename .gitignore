# Secrets
.env
.env.local
*.env.local
cookies.json

# Databases (may contain PII: id_number, phone in booking.db users table)
*.db
*.sqlite
*.sqlite3
backend/booking/*.db
backend/booking/*.sqlite

# Python
.pw/
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/

# Piper voice model binaries — transfer via USB/AirDrop per piper/README.md,
# not git (126MB, and the whole point is avoiding a network transfer).
piper/voicemodelssource/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log
